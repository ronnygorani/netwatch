# Python
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
dist/
build/
venv/
.venv/

# Secrets — never commit the real .env
.env
*.env
!.env.example

# Docker
.docker/

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Terraform state and secrets (Phase 5)
infra/terraform/.terraform/
infra/terraform/*.tfstate
infra/terraform/*.tfstate.backup
infra/terraform/*.tfvars
!infra/terraform/*.tfvars.example

# Logs
*.log
logs/

# Local learning reference files — never committed.
# Heavily-annotated walkthrough docs that mirror the codebase for study.
learn/
learning/

# Design docs — kept local for now, will be committed once reviewed.
docs/
CLAUDE.md

# Test databases
*.db

# ContainerLab runtime state (TLS keys, node flash) — topology YAML is the source
lab/clab-*/

# Local machine convenience scripts (personal paths, not for the public repo)
local/

# Lab setup notes - local only (machine-specific steps, not needed for cloud deploys)
lab/README.md
