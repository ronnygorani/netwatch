# Development and CI tooling — never installed in the production image.

-r requirements.txt

# Testing (httpx is required by Starlette's TestClient)
pytest==8.3.4
httpx==0.28.1

# Lint + format — version must match the CI workflow
ruff==0.15.16
